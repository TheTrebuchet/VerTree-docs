[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "TreeGen"
authors = [{name = "Jan Kulczycki", email = "treegen.dev@gmail.com"}]
dynamic = ["version", "description"]
